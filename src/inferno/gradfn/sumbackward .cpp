#include "sumbackward.h"
#include "inferno/gradengine/engine.h"

namespace Inferno {


	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//
	//  CTORS / DTORS
	//
	//
	//
	//
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


	SumBackward::SumBackward(const Tensor& A) : m_A(A)  {
		set_name("SumBackward");

	}

	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//
	//  Function backward
	//
	//
	//
	//
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	void SumBackward::backward() {
		NoGradGuard guard;

		Tensor g_out = Engine::grad_in(this, 0);

		// result = sum(A)
		// d result / d A = 1 for every element
		// so dL/dA = g_out broadcast to A.shape()
		Tensor g_a = Tensor::ones_like(m_A) * g_out;

		auto na = GetImpl(m_A)->grad_edge();

		if (na) {
			Engine::accumulate(na.get(), 0, g_a);
		}
	}

	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//
	//  Function release
	//
	//
	//
	//
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	void SumBackward::release() {
		// drop references so graph can free
		m_A = Tensor{};
		
	}


	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//
	//  Function get_inputs
	//
	//
	//
	//
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	void SumBackward::get_inputs(std::vector<Tensor>& out) const {
		out.push_back(m_A);
		
	}


}