

namespace Inferno {

	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//
	//  Function cpu_sigmoid
	//
	//
	//
	//
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	template<typename AT, typename RT>
	void cpu_sigmoid(const AT* aptr, RT* outptr, size_t N) {
		for (size_t i = 0; i < N; ++i) {
			RT x = static_cast<RT>(aptr[i]);
			outptr[i] = static_cast<RT>(1) / (static_cast<RT>(1) + std::exp(-x));
		}
	}


}