#pragma once
#include <inferno/core/tensor.h>
#include <inferno/core/ops.h>


namespace Inferno {

	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//
	//  Class Module
	//
	//
	//
	//
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	class Module {

	public:

		virtual ~Module() = default;
		virtual Tensor forward(Tensor& input);

		std::vector<Tensor*> parameters() const;

		void register_parameter(Tensor& tensor);
		void register_module(Module* module);
		void register_buffer(Tensor& tensor);

		void to(const Device& device);

	private:

		std::vector<Module*> _children;
		std::vector<Tensor*> _parameters;
		std::vector<Tensor*> _buffers;


	};

	std::ostream& operator<<(std::ostream& os, const Module& module);


}